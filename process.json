{
  apps : [{
    name        : "DomaHub Server (Prod)",
    script      : "./server/server.js",
    env: {
      "NODE_ENV": "prod",
    },
	watch: ["./server"],
	ignore_watch : ["../node_modules", "../uploads"],
	watch_options: {
		followSymlinks: false
	}
  },
  {
    name        : "DomaHub Server (LE API Prod)",
    script      : "./server/api_server.js",
    env: {
      "NODE_ENV": "prod",
    },
	watch: ["./server"],
	ignore_watch : ["../node_modules", "../uploads"],
	watch_options: {
		followSymlinks: false
	}
  }]
}
