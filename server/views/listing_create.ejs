<!-- user profile page, should not see unless logged in -->

<!DOCTYPE html>

<html>

	<head>
		<title>w3bbi</title>
		<%- include("templates/head_links"); %>
		<link rel="stylesheet" href="/css/listing_create.css" type="text/css" />
		<script type="text/javascript">
			var user = <% if(typeof user != "undefined"){ %><%-JSON.stringify(user)%><% }else{ %>""<% } %>;
			var message = <% if(typeof message != "undefined"){ %><%-JSON.stringify(message)%><% }else{ %>""<% } %>;
		</script>
		<script src="/js/routes/client_listing_create.js" type="text/javascript"></script>
	</head>

	<body>

		<%- include("templates/navbar", {user: user}); %>


		<div id="new_listing_wrapper">
			<form id="listing_form" action="/listing/create" method="post" novalidate>
				<input id="listing_form_domain_name" type="url" name="domain_name" placeholder="https://myawesomedomain.com" />
				<input id="listing_form_description" type="text" name="description" placeholder="The best website ever!" />
				<input id="submit" type="submit" value="Create new listing!" />
			</form>

			<div id="batch_listing_show">Create multiple listings</div>

			<div id="batch_form_wrapper">
				<p>You can upload a .csv file in the following format:</p>
				<form id="batch_listing_form" action="/listing/create/batch" method="post" enctype="multipart/form-data">
					<input id="listing_form_csv" type="file" name="csv" accept=".csv" />
					<input id="submit" type="submit" value="Create new listings!" />
				</form>
				<progress></progress>
			</div>

			<div id="instruction_wrapper">
				<div class="verification_method_wrapper" id="hash_txt">

				</div>
				<div class="verification_method_wrapper" id="hash_html">

				</div>
				<div class="verification_method_wrapper" id="hash_meta">

				</div>
				<div class="verification_method_wrapper" id="hash_htaccess">
					<p>Copy the below code into the .htaccess file of your website to prove that you own the domain name!</p>
					<p>What is the .htaccess file?</p>
					<p>An .htaccess file is a list of various settings for your website. By entering in your unique code below to your .htaccess file, we can create a link that will prove the ownership of this website name.</p>
					<p>How do I edit my .htaccess file?</p>
					<code>Header set w3bbi "<span id="hash_code"></span>"</code>
				</div>
			</div>
		</div>
	</body>

</html>
