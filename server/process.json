{
  apps : [{
    name        : "DomaHub Test LetsEncrypt",
    script      : "./letsencrypt_server.js",
    env: {
      "NODE_ENV": "dev",
    },
	watch: ["../server"],
	ignore_watch : ["../node_modules", "../uploads"],
	watch_options: {
		followSymlinks: false
	}
  }]
}