{
  apps : [{
    name        : "DomaHub Server (Test)",
    script      : "./server.js",
    env: {
      "NODE_ENV": "dev",
    },
	watch: ["../server"],
	ignore_watch : ["../node_modules", "../uploads"],
	watch_options: {
		followSymlinks: false
	}
  },
  {
    name        : "DomaHub Server (LE API Test)",
    script      : "./api_server.js",
    env: {
      "NODE_ENV": "dev",
    },
	watch: ["../server"],
	ignore_watch : ["../node_modules", "../uploads"],
	watch_options: {
		followSymlinks: false
	}
  }
  ]
}